name: ffbe_patcher
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  path_provider: ^2.1.4
  dio: ^5.7.0
  json_annotation: ^4.9.0
  ffi: ^2.1.3
  url_launcher: ^6.3.0

  path: any
  file_picker: ^8.1.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
